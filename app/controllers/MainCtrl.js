app.controller("MainCtrl", 
  ["$scope",
   "$firebaseArray",
  function($scope, $firebaseArray) {
    // var ref = new Firebase("https://ma-compare.firebaseio.com/"); 

    // Data from firebase 
    // $scope.something = $firebaseArray(ref);

    
  }
]);